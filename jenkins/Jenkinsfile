pipeline {
    agent any

    stages {
        stage ('Plan') {
            steps {
                bat 'terraform -version'
                // terraform init
                // terraform plan
            }
        }

        stage ('Apply') {
            steps {
                input ('Approve?')
                bat 'terraform -version'
                // terraform init
                // terraform apply -auto-approve
            }
        }
    }
}

