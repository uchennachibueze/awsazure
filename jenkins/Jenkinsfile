pipeline {
    agent any

    stages {
        stage ('Plan') {
            steps {
                terraform init
                terraform plan
            }
        }

        stage ('Apply') {
            steps {
                input ('Approve?')
                terraform init
                terraform apply -auto-approve
            }
        }
    }
}